{% for item in csvfile_vars.split("\n") %}
{%   if loop.index !=1 %}
{%     set list = item.split(",") %}

  - name: "{{ list[0]|trim() }}"
    serial: "{{ list[0]|trim() }}"
    node_id: "{{ list[1]|trim() }}"
    switch: "{{ list[2]|trim() }}"
    state: query
{%      endif %}
{%  endfor %}