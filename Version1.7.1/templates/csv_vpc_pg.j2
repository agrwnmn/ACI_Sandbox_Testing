{% for item in csvfile_vars.split("\n") %}
{%   if loop.index !=1 %}
{%     set list = item.split(",") %}

  - name: "{{ list[12]|trim() }}"
    protection_group: "{{ list[12]|trim() }}-{{ list[13]|trim() }}"
    switch_1_id: "{{ list[12]|trim() }}"
    switch_2_id: "{{ list[13]|trim() }}"
    protection_group_id: "{{ list[12]|trim() }}"
    state: present
{%      endif %}
{%  endfor %}