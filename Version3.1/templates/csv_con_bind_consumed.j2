{% for item in csvfile_vars.split("\n") %}
{%   if loop.index !=1 %}
{%     set list = item.split(",") %}

  - name: "{{ list[0]|trim() }}_{{ list[13]|trim() }}"
    tenant: "{{ list[1]|trim() }}"
    ap: "{{ list[0]|trim() }}-{{ list[15]|trim() }}"
    epg: "{{ list[0]|trim() }}_{{ list[3]|trim() }}-epg"
    contract: "{{ list[13]|trim() }}"
    contract_type: consumer
    state: present
{%      endif %}
{% endfor %}
